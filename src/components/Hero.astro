---
import { gsap } from "gsap";
import lottie from "lottie-web";
---

<section class="h-screen flex flex-col items-center justify-center text-center">
  <div id="lottie-logo" class="w-32 h-32 mb-6"></div>
  <h1 class="hero-title text-5xl font-bold mb-3">Hola, soy Uziel 👋</h1>
  <p class="text-gray-400 text-lg">Ingeniero en Sistemas Computacionales</p>
  <a
    href="/projects"
    class="mt-8 px-6 py-3 bg-blue-600 rounded-lg hover:bg-blue-700 transition"
  >
    Ver mis proyectos
  </a>

  <!-- 👇 Este script sí se ejecuta en el navegador -->
  <script>
  import { gsap } from "gsap";
  import lottie from "lottie-web";

  document.addEventListener("DOMContentLoaded", () => {
    const title = document.querySelector(".hero-title");
    const animContainer = document.querySelector("#lottie-logo");

    if (title && animContainer) {
      gsap.fromTo(title, { opacity: 0, y: 30 }, { opacity: 1, y: 0, duration: 1 });

      lottie.loadAnimation({
        container: animContainer,
        renderer: "svg",
        loop: true,
        autoplay: true,
        path: "/logo.json"
      });
    }
  });
</script>
</section>
